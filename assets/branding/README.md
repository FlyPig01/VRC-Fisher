# Branding assets

`VRC-Fisher.svg` is the first-pass application icon concept. It is an original
vector source asset for visual review only. It is not referenced by the WinUI
application or Inno Setup yet, and no `.ico` has been generated. After the
visual design is approved, export the required Windows icon sizes from this
source and update the packaging manifest in a separate change.
