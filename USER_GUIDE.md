# VRC-Fisher 使用手册

> 当前还没有可下载的正式软件或模型。本文定义首个正式版本的安装与使用方式；GitHub Releases 出现 `VRC-Fisher-Setup-x64.exe` 后才可按本手册操作。

## 1. 软件用途

VRC-Fisher 在 Windows 上观察用户选择的完整显示器，识别 VRChat 钓鱼提示和小游戏，并在用户明确启动后控制鼠标完成钓鱼。软件提供“仅观察”和“自动运行”两种模式。

使用前请确认目标 VRChat 世界允许此类工具，并遵守 VRChat 及相关世界的规则。软件不保证兼容所有钓鱼世界或 UI 版本。

## 2. 安装

从本仓库 GitHub Releases 的 `app-v*` 版本下载：

```text
VRC-Fisher-Setup-x64.exe
```

一个 Setup 内完成所有选择：

1. 选择简体中文或 English。
2. 选择软件安装目录。
3. 选择 CPU-only 或 DirectML。
4. 选择是否在安装后立即下载模型。
5. 选择是否创建桌面快捷方式。

安装目录不固定到 C 盘，也不取决于 Setup 下载到了哪里。请选择当前账户可写的普通目录，例如：

```text
D:\Tools\VRC-Fisher
E:\Apps\VRC-Fisher
```

不要选择已有无关文件的目录，也不要选择需要管理员权限才能写入的受保护目录。

## 3. 选择运行组件

| 组件 | 适合情况 | 可用设备 |
|---|---|---|
| CPU-only | 依赖更少；无可用 GPU，或 DirectML 影响游戏时 | `Auto`、`CPU` |
| DirectML | 使用支持 DirectX 12 的 NVIDIA、AMD 或 Intel GPU | `Auto`、`CPU`、`GPU` |

默认推荐 DirectML，但它不保证在每台电脑上都比 CPU 快。首次使用应分别测试 `Auto` 和 `CPU`，以 VRChat 帧率、识别延迟和稳定性决定。

设备选项含义：

- `Auto`：DirectML 组件优先使用 GPU，初始化失败时回退 CPU；CPU-only 组件直接使用 CPU。
- `CPU`：强制使用 CPU。
- `GPU`：强制使用 DirectML；不可用时显示错误，不静默回退。

软件会显示实际使用的 Provider。CPU-only 与 DirectML 使用同一套模型，切换组件或设备不需要重新训练或下载模型。当前不需要 CUDA。

## 4. 模型管理

软件需要同一版本的两个模型：

```text
<安装目录>\models\locator.onnx
<安装目录>\models\minigame.onnx
```

未安装模型时软件仍可打开，但“仅观察”和“自动运行”不可用。打开“模型”页面可以：

- 下载或更新兼容模型；
- 查看模型版本、大小和校验状态；
- 取消失败或过慢的下载并重试；
- 随时删除两个模型，释放空间而不卸载软件；
- 删除后重新下载。

模型从本仓库的 `models-v*` GitHub Release 下载。下载完成并通过完整性校验后才替换旧版本；中断或校验失败不会破坏原有模型。

## 5. 首次使用

1. 启动 VRChat 并进入目标钓鱼世界。
2. 打开 VRC-Fisher 的“模型”页面，确认两个模型均通过校验。
3. 打开“设置”，选择显示 VRChat 的完整显示器。
4. 选择运行设备，并确认软件显示的实际 Provider。
5. 回到“运行”，先启动“仅观察”。
6. 观察多个完整钓鱼流程，确认提示、小游戏、成功和失败状态切换正确。
7. 停止观察后，再明确点击“自动运行”。

“仅观察”只捕获和识别画面，不发送鼠标输入。“自动运行”会实际控制鼠标，两种状态在界面上必须明显不同。

运行期间按 `F8` 紧急停止。遇到错误识别、窗口切换、画面卡住或任何意外行为时，立即按 `F8`；软件应停止并释放鼠标。

## 6. 页面

| 页面 | 用途 |
|---|---|
| 运行 | 仅观察、自动运行、停止、当前阶段、VRChat 状态和实际 Provider |
| 模型 | 下载、更新、校验、删除、版本和大小 |
| 设置 | 显示器、设备、VRChat 窗口、识别阈值和超时 |
| 诊断 | 推理延迟、帧龄、资源占用、日志和可选画面预览 |

诊断预览默认关闭。正常使用不需要持续显示完整屏幕画面；只有排查问题时才临时开启预览。

界面支持简体中文、English、明暗主题和系统强调色。两种语言资源随软件本体安装，语言切换不访问网络、不下载单独语言包。

## 7. 文件位置

所有软件运行数据都位于安装目录：

```text
<安装目录>\
  VrcFisher.exe
  config\
  models\
  downloads\
  logs\
  artifacts\
  USER_GUIDE.md
```

| 内容 | 位置 |
|---|---|
| 用户设置 | `config\user.json` |
| 模型 | `models\` |
| 下载暂存 | `downloads\` |
| 日志 | `logs\vrc-fisher.log` |
| 性能摘要 | `artifacts\runtime-metrics.json` |
| 失败截图 | `artifacts\failures\` |

软件不会把这些运行数据写入 C 盘的隐藏用户目录。作为标准安装版，Windows 仍会保存卸载登记；只有用户勾选快捷方式时，系统才会在桌面或开始菜单创建快捷方式。

## 8. 更新

应用和模型分开更新：

- 软件更新：从新的 `app-v*` Release 下载新版 Setup，安装到原目录。
- 模型更新：在软件“模型”页面执行，不重新安装软件。
- 删除模型：在“模型”页面删除，不影响软件本体。

首版没有后台自动更新或增量更新。软件可以提示新版并打开 Release 页面，但不会自行替换程序。

覆盖安装保留模型、用户设置、日志和诊断文件。切换 CPU-only 与 DirectML 也通过同一个 Setup 完成，并保留这些数据。

## 9. 卸载

使用 Windows 的“已安装的应用”卸载 VRC-Fisher。卸载会删除安装目录内的软件、模型、设置、日志和诊断文件；操作前请保留确实需要的数据。

只需释放模型空间时，请在软件“模型”页面删除模型，不要卸载整个软件。

## 10. 故障处理

| 问题 | 处理 |
|---|---|
| 运行按钮不可用 | 在“模型”页面检查两个模型是否存在并通过校验 |
| GPU 不可用 | 改用 `Auto` 或 `CPU`，或重新运行 Setup 安装 CPU-only |
| VRChat 明显掉帧 | 关闭诊断预览，比较 `Auto` 与 `CPU` |
| 状态识别错误 | 立即按 `F8`，停止自动运行并保留日志和失败截图 |
| 下载失败 | 检查网络后重试；旧模型应保持可用 |
| 安装目录无法写入 | 重新安装到当前账户可写的普通目录 |
