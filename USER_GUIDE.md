# VRC-Fisher 使用手册

> 本地已准备 `app-v0.1.3` 软件安装器和 `models-v0.1.0` 模型发行物，GitHub Release 资产仍需由维护者上传。当前 round3 模型已作为可发布模型固化并允许自动输入，但现场资源占用和完整自动流程仍需持续实机验收。

## 1. 软件用途

VRC-Fisher 在 Windows 上自动查找 `VRChat.exe` 的主窗口，识别咬钩感叹号和钓鱼小游戏，并在用户启动后控制鼠标完成钓鱼。软件不允许选择其他进程、窗口或显示器，不提供与产品无关的观察模式。

一轮自动操作固定为：

```text
左键抛竿
  -> 识别到咬钩感叹号后左键收钩；始终不可见时按用户等待时间兜底一次
  -> 识别 minigame_panel
  -> 按住或松开左键，控制 catch_zone 上下移动并包住 moving_target
  -> minigame_panel 连续消失后左键收杆
  -> 下一轮
```

软件不额外识别成功、失败、轨道或进度条。小游戏 UI 连续消失就是结束信号。

当前只支持 [Fins Fishing](https://vrchat.com/home/world/wrld_ae001ea3-ed05-42f0-adf2-3d47efd10a77/info)。使用前请确认该世界允许此类工具，并遵守 VRChat 及世界规则；世界 UI 更新后可能需要更新模型。

## 2. 安装

从本仓库 GitHub Releases 的 `app-v*` 版本下载：

```text
VRC-Fisher-Setup-x64.exe
```

一个 Setup 内完成所有选择：

1. 确认安装器语言。首次安装会按 Windows 界面语言从 20 种内置语言中预选；没有对应语言时默认 English，也可以手动改选。安装器后续页面会使用这里选定的语言。
2. 选择软件安装目录。
3. 选择是否在安装后立即下载模型。
4. 选择是否创建桌面快捷方式。

安装目录不固定到 C 盘，也不取决于 Setup 下载到了哪里。请选择当前账户可写的普通目录，例如：

```text
D:\Tools\VRC-Fisher
E:\Apps\VRC-Fisher
```

不要选择已有无关文件的目录，也不要选择需要管理员权限才能写入的受保护目录。

## 3. 选择运行设备

Setup 只安装一套 DirectML 版程序，其中同时提供三种运行选项：

| 选项 | 行为 |
|---|---|
| `Auto` | 优先使用 DirectML GPU；初始化失败时回退 CPU，并显示回退原因 |
| `GPU` | 强制使用 DirectML；不可用时明确报错，不静默回退 |
| `CPU` | 使用同一安装包内的 CPU 执行后端 |

默认推荐 `Auto`，但 GPU 不保证在每台电脑上都比 CPU 快。首次使用可分别测试 `Auto` 和 `CPU`，以 VRChat 帧率、识别延迟和稳定性决定。

设备选项含义：

- `Auto`：优先使用 GPU，初始化失败时回退 CPU。
- `CPU`：强制使用 CPU。
- `GPU`：强制使用 DirectML；不可用时显示错误，不静默回退。

软件会分别显示用户选择和最近一次成功运行实际使用的 CPU / DirectML 设备，不向普通用户暴露内部 Provider 名称。当前进程尚未成功运行时显示“暂无运行记录”；切换设备后显示“等待下次运行确认”。三种选项使用同一套模型，切换设备不需要重新训练或下载模型。当前不需要 CUDA。

识别频率始终自动调节。软件只在现有 ONNX 调用前后计时，不增加截图或模型推理；locator、双模型和缓存小游戏分别丢弃 10 次预热并累计至少 30 个有效样本，每 5 秒检查一次 P95。性能变慢时立即放宽间隔，连续稳定 30 秒后才逐步加快，避免频率来回跳动。

自动范围固定为：等待感叹号 `80–250 ms`、等待小游戏 `80–250 ms`、小游戏控制 `33–67 ms`、面板复查 `250–1000 ms`。小游戏已经到 `67 ms` 仍超出性能预算时，软件会显示性能不足警告，不会继续降低频率掩盖问题。自动调节没有开关或手动频率设置。

自动画像只会用于完全相同的实际后端、CPU/GPU、模型版本和捕获分辨率。首次运行或其中任一项变化后会重新采样，已匹配的画像保存在安装目录内。

## 4. 模型管理

软件需要同一版本的两个模型：

```text
<安装目录>\models\locator.onnx
<安装目录>\models\minigame.onnx
<安装目录>\models\MODEL_CARD.md
<安装目录>\models\MODEL_LICENSE.txt
```

未安装模型时软件仍可打开，但不能启动自动钓鱼。打开“模型”页面可以：

- 直接查看 `locator.onnx` 与 `minigame.onnx` 各自用途和状态；
- 自动检查兼容模型新版，并明确显示发布者 `FlyPig01/VRC-Fisher`；
- 模型缺失或损坏时显示蓝色“下载”，存在新版时显示绿色“更新”，已是最新版时显示“删除”；
- 下载、更新和删除都需要再次确认，下载与更新始终处理整套兼容模型；
- 查看模型文件夹、两个模型的总占用、大小和校验状态；
- 切换到其他页面后继续下载，返回模型页时恢复显示进度和当前速度；
- 取消失败或过慢的下载并重试；
- 随时删除两个模型及其模型卡、许可证和安装记录，释放空间而不卸载软件；
- 删除后重新下载。

模型从本仓库的 `models-v*` GitHub Release 下载。两个 ONNX、模型卡和模型许可证均按清单校验大小与 SHA-256，四个文件全部通过后才替换旧版本；中断或校验失败不会破坏原有模型。断网恢复后，只要模型清单未变化且断点文件有效，就会继续下载，不限制断网持续时间；如果期间发布了新模型，软件会丢弃旧版本断点并重新下载新版，绝不拼接两个版本。模型卡或许可证缺失、损坏时，该模型版本不会被视为可用。Release 只是安装下载渠道；同版本的 `.pt`、ONNX、模型卡、许可证和源码清单也完整保存在源码仓库的 `models/vX.Y.Z/`，可用于审计、修改和继续训练。

## 5. 首次使用

1. 打开软件的“使用指南”页，点击本地世界封面查看该世界的官方网页信息，再从 VRChat 内进入当前支持的钓鱼世界。
2. 打开 VRC-Fisher 的“模型”页面，确认两个模型均通过校验。
3. 等待“运行”页显示已找到 VRChat；软件只会连接 VRChat 主窗口。
4. 在“设置”中选择运行设备和工作模式；选择后立即生效，不需要重启软件。默认运行模式不显示识别框；调试模式显示识别框和置信度数字。
5. 感叹号可能出现在屏外时，启用“屏外感叹号兜底”。等待时间范围为 `5–30` 秒，默认 `15` 秒；该功能默认禁用。
6. 切回 VRChat，按启动/停止热键开始自动钓鱼。默认是 `F8`。
7. 再按一次相同热键停止；停止时软件会释放可能按住的左键。热键可自由设置为 `F1–F24`，或带 `Ctrl / Alt / Shift` 的字母、数字组合；修改经过“开始修改、按键捕获、最终确认”三个步骤，并在临时冲突检测通过后立即生效。

感叹号可能位于捕获画面之外，因此不能要求模型在每一轮都看到它。兜底等待时间应由用户按目标世界的咬钩节奏调整。计时到达后每轮只允许兜底点击一次；如果小游戏仍未出现，软件应超时复位，而不是持续点击。该功能是屏外提示的折中方案，不保证所有世界都能可靠触发小游戏。

启动/停止热键只负责这两个动作：未运行时按下会启动自动钓鱼，运行中按下会停止并释放左键。它不是长期注册的系统全局热键，只在 VRChat 位于前台时响应。切换窗口、最小化、锁屏、休眠或退出 VRChat 会立即释放鼠标并完整停止；返回游戏后必须重新按热键启动。遇到错误识别、画面卡住或任何意外行为时，再按一次当前热键停止。

按下启动热键后，运行模式和调试模式都会立即在 VRChat 客户区右上角显示“正在启动”；首帧准备成功后切换为“按当前热键停止”。启动失败时显示红色失败提示 6 秒，并释放鼠标输入；完整错误保留在软件窗口和日志中，不会主动抢走 VRChat 焦点。提示左侧同时显示当前模式：运行模式为三角形，调试模式为虫形调试图标；设置页的模式选项只显示文字。覆盖层不接收鼠标输入，不会进入 VRChat 的 WGC 捕获画面；VRChat 不在前台、最小化或正常停止时自动隐藏。

调试模式以固定 `15 Hz` 刷新四类最终检测框。框旁只显示 `0.00-1.00` 的置信度数字，不显示类别名称；颜色用于区分类别。显示坐标采用约 `90 ms` 的指数平滑，并容忍连续两次推理漏检，以减少抖动和闪烁。防抖只改变屏幕显示，状态机和鼠标控制始终使用当次未平滑的原始检测结果。

## 6. 页面与语言

| 页面 | 用途 |
|---|---|
| 运行 | 查看 VRChat 进程、当前阶段、状态、模型和实际运行设备；页面不提供启停按钮 |
| 模型 | 查看两个模型的用途和状态；检查新版并下载、更新或删除完整模型包；查看模型目录、总占用和明确的发布源 |
| 使用指南 | 查看当前支持的世界封面并打开官方世界页，按三步完成首次启动 |
| 设置 | 选择界面语言、工作模式、运行设备和启动/停止热键，设置屏外感叹号兜底，打开软件目录 |

侧栏固定显示且不可收起。运行页把处理器、图形设备、内存与系统、推理状态分区显示；软件启动时检测一次 CPU、GPU、显存、驱动、内存和 Windows，并按系统 build 号区分 Windows 10/11、按 DXGI 设备去重。实际推理设备只在成功建立运行会话后记为“最近一次运行”，停止后保留到软件退出。软件不持续轮询利用率，也不播放捕获画面或显示内部推理列表；需要现场检查识别结果时使用调试模式的 VRChat 覆盖层。

软件提供 20 种实际界面语言，语言列表始终使用各语言自己的名称，例如 `English`、`简体中文` 和 `日本語`。软件不提供“跟随系统”选项：首次启动采用安装器最终选定的语言；没有有效安装器语言记录时使用 English。之后可在“设置”页切换，选择后当前页面立即刷新并写入安装目录的 `config/user.json`，后续启动保持用户选择。全部语言资源都已编译进安装包内的 `VrcFisher.pri`，不联网下载单独语言包；新增翻译仍需发布新的软件版本。

## 7. 文件位置

所有软件运行数据都位于安装目录：

```text
<安装目录>\
  release.json
  program\\VrcFisher.exe
  config\
  models\
  downloads\
  logs\
  USER_GUIDE.md
```

| 内容 | 位置 |
|---|---|
| 用户设置 | `config\user.json` |
| 自动调频画像 | `config\performance-profiles.json` |
| 模型 | `models\` |
| 下载暂存 | `downloads\` |
| 日志 | `logs\vrc-fisher.log` |

软件不会把这些运行数据写入 C 盘的隐藏用户目录。作为标准安装版，Windows 仍会保存卸载登记；只有用户勾选快捷方式时，系统才会在桌面或开始菜单创建快捷方式。

## 8. 更新

应用和模型分开更新：

- 软件更新：从新的 `app-v*` Release 下载新版 Setup，安装到原目录。
- 模型更新：在软件“模型”页面执行，不重新安装软件。
- 删除模型：在“模型”页面删除，不影响软件本体。

首版没有软件版本自动检查、后台自动更新或增量更新。用户需要自行查看项目 `app-v*` Release 并下载新版 Setup；模型页打开时会单独检查最新兼容的 `models-v*` Release。

覆盖安装保留模型、用户设置和日志。CPU、GPU 和自动模式都在软件设置内切换，不需要重新安装。

## 9. 卸载

使用 Windows 的“已安装的应用”卸载 VRC-Fisher。卸载会删除安装目录内的软件、模型、设置和日志；操作前请保留确实需要的数据。

只需释放模型空间时，请在软件“模型”页面删除模型，不要卸载整个软件。

## 10. 故障处理

| 问题 | 处理 |
|---|---|
| 启动热键无响应 | 在“模型”页面检查两个模型是否存在并通过校验，并确认 VRChat 已启动 |
| GPU 不可用 | 改用 `Auto` 或 `CPU`；不需要重新安装软件或模型 |
| VRChat 明显掉帧 | 按当前启动/停止热键停止，再比较 `Auto` 与 `CPU` |
| 显示设备性能不足 | 按当前启动/停止热键停止；比较 `CPU` 与 `Auto`，并查看“用户选择 / 最近一次运行”。软件不会突破 67 ms 小游戏上限 |
| 状态识别错误 | 再按一次当前启动/停止热键，停止自动钓鱼并保留日志 |
| 没有右上角停止提示或显示覆盖层错误 | 软件会拒绝启动或自动停止并释放鼠标；重启 VRC-Fisher 后重试，并保留 `logs/vrc-fisher.log` |
| 感叹号经常在屏外 | 启用“屏外感叹号兜底”并调整等待时间；不要把它设成无依据的短时间 |
| 兜底点击后小游戏未出现 | 当前世界的等待时间不合适，或该世界不适用固定等待；停止自动运行并重新验证 |
| 下载失败 | 检查网络后重试；旧模型应保持可用 |
| 安装目录无法写入 | 重新安装到当前账户可写的普通目录 |

## 11. 许可证与使用范围

VRC-Fisher 的原创应用代码采用 MIT License，允许个人、研究、非营利和商业使用，也允许修改与再发布。安装包中的第三方运行库遵循各自许可证。

本项目官方 ONNX 模型由 Ultralytics YOLO11 训练链产生，按上游标注的 AGPL-3.0 发布，不属于 MIT。AGPL 允许收费和商业使用，但分发带官方模型的完整产品或通过网络提供相应服务时，需要履行对应源码、许可证和修改说明等义务。删除官方模型时，软件会同时删除其模型卡和模型许可证；替换为来源合法、许可证兼容的独立模型时，模型自身的许可证由其来源决定。

安装目录包含 `LICENSE`、`THIRD_PARTY_NOTICES.md` 和 `licenses\AGPL-3.0.txt`。从软件内安装官方模型后，`models\` 还包含该版本的 `MODEL_CARD.md` 与 `MODEL_LICENSE.txt`。录屏、训练图片、标注和数据集不随项目公开，也不因应用代码使用 MIT 而获得公共使用许可。

这些许可证只处理版权许可，不代表 VRChat 或目标世界允许自动化行为。使用者应自行确认并遵守平台与世界规则。
