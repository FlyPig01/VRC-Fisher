# VRC-Fisher 使用手册

VRC-Fisher 当前只支持 VRChat 世界 [Fins Fishing](https://vrchat.com/home/world/wrld_ae001ea3-ed05-42f0-adf2-3d47efd10a77/info)。世界 UI 更新后可能需要新模型；使用者仍需遵守 VRChat 和世界规则。

## 1. 安装

从 `app-v*` GitHub Release 下载 `VRC-Fisher-Setup-x64.exe`。安装器按 Windows 界面语言预选语言，没有匹配时使用 English；用户可以手动更改。随后选择可写安装目录、桌面快捷方式和是否立即下载模型。

安装目录不固定到 C 盘，也与 Setup 下载位置无关。Setup 自带 .NET、WinUI 3、ONNX Runtime DirectML 和 20 种语言，不要求 Python、CUDA 或 Miniconda。

## 2. 模型

软件需要同一版本的 `locator.onnx` 和 `minigame.onnx`。每个模型条目会在用途说明下方显示已安装版本；模型页还会显示状态、总占用、发布源和模型目录：

- 缺失或损坏：蓝色“下载”；
- 有兼容新版：绿色“更新”；
- 已是最新版：显示“删除”。

下载、更新和删除都需要确认。下载会校验大小和 SHA-256，成功后才替换旧版本；中断不会破坏已安装模型，模型版本变化时会放弃旧断点并下载新版。离开模型页不会停止下载。

## 3. 开始钓鱼

1. 进入 Fins Fishing 并拿起钓竿。
2. 调整视角，让感叹号和小游戏组件都能出现在画面内。
3. 打开 VRC-Fisher，确认 VRChat、两个模型和运行设备均可用。
4. 在设置中选择运行/调试、`Auto / GPU / CPU` 和热键。
5. 保持 VRChat 在前台，按热键启动；默认 `F8`。
6. 再按一次热键停止。切出、最小化、锁屏或退出 VRChat 也会立即停止并释放左键，返回后不会自动恢复。

运行页不提供启停按钮。`Auto` 优先 DirectML GPU，失败时回退 CPU；`GPU` 强制 DirectML，失败会报错；`CPU` 使用同一安装包的 CPU 后端。切换设备不需要重新训练或下载模型。

感叹号可能位于画面外时，可启用“屏外感叹号”。等待范围为 `5–30` 秒，默认 `15` 秒且功能默认关闭；每轮到时最多兜底点击一次。

## 4. 运行和调试

运行和调试模式都会在 VRChat 右上角显示紧凑状态卡，主行依次提示抛竿、等鱼上钩、和鱼搏斗中、收竿等当前阶段，次行显示停止热键。调试模式额外绘制识别框和 `0.00–1.00` 置信度；覆盖层点击穿透，不捕获鼠标，也不会进入识别画面。框超过 `150 ms` 会隐藏。

识别频率由软件按设备性能在固定范围内自动调整，不提供手动频率选项。小游戏目标约为 `15/20/25 Hz`；性能不足时会明确警告。

## 5. 页面

| 页面 | 用途 |
|---|---|
| 运行 | VRChat、阶段、模型、硬件和实际运行设备状态 |
| 模型 | 下载、更新、删除和打开模型目录 |
| 使用指南 | 查看支持世界并打开官方世界页 |
| 设置 | 语言、运行/调试、设备、热键、屏外感叹号和软件目录 |

语言首次使用安装器选择，之后在设置页立即切换。全部语言内置，不下载语言包。

## 6. 文件与更新

```text
<安装目录>/
  program/      软件
  config/       设置和性能画像
  models/       模型与许可证
  downloads/    下载暂存
  logs/         运行与调试日志
```

应用更新：下载新版 `app-v*` Setup 并覆盖安装；设置、模型和日志会保留。模型更新：直接在模型页完成，不重新安装软件。

卸载请使用 Windows“已安装的应用”。只需释放模型空间时，在模型页删除模型即可。

## 7. 常见问题

| 问题 | 处理 |
|---|---|
| 热键无响应 | 确认 VRChat 在前台、模型就绪且热键未冲突 |
| GPU 不可用 | 改用 `Auto` 或 `CPU`，无需重装 |
| VRChat 掉帧 | 停止后比较 `Auto` 与 `CPU` |
| 感叹号经常在屏外 | 启用屏外感叹号并按世界节奏调整时间 |
| 状态或输入异常 | 再按热键停止，保留对应 `logs/*/current.log` |
| 下载失败 | 检查网络后重试；旧模型应继续可用 |
| 安装目录不可写 | 重新安装到当前账户可写的普通目录 |

## 8. 当前限制

软件会在小游戏结束后保持左键释放并等待 `1 秒`，随后收杆；收杆点击完成后等待 `2 秒`，再抛下一竿。低识别帧率控制增强已完成现场验证。locator 与 minigame 模型仍可能漏检或框选不准；模型质量仍是当前唯一未关闭问题。
